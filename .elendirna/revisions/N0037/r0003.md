---
baseline: N0037@r0002
created: 2026-05-15T09:29:58.308881300+09:00
---

## Delta

## r0002 후속: `elf help --json` 자연 첫시도 결함 (외부 보고)

[Change]
- src/main.rs: `Cli`에 `disable_help_subcommand = true` 추가, `Help` variant에 `alias = "help"` 추가
- 이제 `elf help` / `elf help --json` 모두 우리 `guide` 핸들러로 라우팅됨

[Impact]
- r0002에서 `Help → guide`로 rename해 panic은 회피했으나, 새 사용자가 자연스럽게 친 `elf help --json`이 clap 빌트인 help subcommand로 잡혀 "unrecognized subcommand '--json'"로 실패하는 UX 결함이 외부 환경(`C:\Users\fo115\doc\my-home-vault`)에서 보고됨
- 빌트인 `help <SUB>` subcommand는 disable됐지만 `elf --help` / `elf <CMD> --help` flag는 그대로 동작 — 사용자가 잃는 건 `elf help entry` 한 패턴뿐, `elf entry --help`로 대체 가능
- 검증: cargo test --lib 62 passed, `elf help --json` / `elf guide --json` / `elf --help` 3종 수동 확인 OK