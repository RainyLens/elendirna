---
baseline: N0039@r0000
created: 2026-04-16T16:25:28.066474500+09:00
---

## Delta

## 구현 범위 (N0038 설계 기반)

→ see N0038 (설계 전체 맥락)

### 변경 대상 파일
- src/mcp/mod.rs — 핵심 변경
- src/cli/serve.rs — 필요 시 minor

### ElfMcpServer 구조체 변경
local_vault: Option<PathBuf> 필드 추가 (인메모리, 프로세스 스코프)

### session_start 변경
파라미터: local_vault: Option<String> 추가
- 미등록 상태 → 등록 후 resolved 경로 반환
- 이미 등록 + 동일 경로 → ok (idempotent)
- 이미 등록 + 다른 경로 → conflict 응답 (confirm=true 요구)
- local_vault 없음 → find_vault_root(CWD) 시도, 실패 시 미등록 유지

### vault 파라미터 추가 대상 tool
entry_list, entry_show, entry_new, entry_status,
revision_add, bundle, query, sync_record, validate

### vault 해석 함수 (공통 추출)
resolve_tool_vault(explicit: Option<String>, local: Option<PathBuf>, primary: PathBuf) -> Result<PathBuf>
1. explicit Some → 절대경로 / 'global' / 'local' / alias 해석
2. explicit None + local Some → local 반환
3. explicit None + local None → primary 반환

### bundle 링크 해석
linked entry 탐색 시 동일 vault_root 사용
없으면 ElfError::NotFound + validate 권장 메시지