---
baseline: N0055@r0001
created: 2026-04-24T15:31:14.828941500+09:00
---

## Delta

[Change] Phase 1 MCP multi-vault review follow-up implemented in src/mcp/mod.rs: vault='local' now resolves to the MCP server's startup vault instead of process cwd; resolved vault paths are normalized/canonicalized and rejected unless they contain .elendirna/config.toml; session vault lock poison now returns an internal error instead of silently falling back. Added resolver unit tests for local-vs-cwd, explicit-over-session precedence, and non-vault rejection.

[Impact] Reduces risk of writing to the wrong vault in MCP clients whose cwd differs from the project, and blocks aliases that point outside initialized Elendirna vaults. cargo check and full cargo test pass.