---
baseline: N0062@r0003
created: 2026-04-27T11:02:10.517433400+09:00
---

## Delta

## 최종 확정 범위 (2026-04-27)

[Change] WritePolicy 제거, 나머지 복원.

WritePolicy enum이 장황하다는 판단. `VaultOrigin`에서 직접 파생하면 별도 타입 없이도 동일한 동작 가능.

### 확정 범위

**포함:**
1. `"local"` alias 수정 — `resolve_named_vault("local")` → session_local_vault (미등록 시 에러)
2. `VaultOrigin` / `VaultResolution` 타입 정의 + serve.rs provenance 캡처
3. 모든 응답에 `vault_resolution` 필드 추가
4. `FallbackGlobal` 시 `fallback: true + warning` 반환
5. mutating tool에 `#[serde(default)] confirm: bool` — write guard는 `origin == FallbackGlobal && !confirm` 인라인 체크로 충분, WritePolicy 타입 불필요

**제외:**
- `WritePolicy` enum — VaultOrigin 직접 참조로 대체
- validate write guard — maintenance 분류, 차기 결정

### 구현 순서 (최종)
1. `VaultOrigin`, `VaultResolution` 정의 (`src/vault/mod.rs`)
2. serve.rs provenance 캡처 → `ElfMcpServer::new` signature 변경
3. 테스트 작성 (resolver 케이스 + confirm 동작)
4. `resolve_tool_vault` → `Result<VaultResolution>` 리팩터
5. 응답에 `vault_resolution` + fallback 필드 추가
6. mutating tool에 `confirm: bool` + FallbackGlobal 인라인 체크