---
baseline: N0065@r0001
created: 2026-04-30T15:15:36.128759600+09:00
---

## Delta

## codex/gemini 상호검증 종합 (2026-04-30)

### 일치점
- v0.5.2 patch 분량 적합 — 신규 데이터 모델 없이 read-path만 확장
- sqlite SSoT 뒤집기 미루기 (둘 다 적극 동의). gemini는 "sqlite 형상관리하면 binary 충돌 지옥, gitignore하면 동기화 불가" 추가 근거 + premature optimization 진단
- bundle에 sync_history 자동 포함 + 요약형 + limit 5~10

### 내 권고 수정 (codex/gemini 입력)
- **co_entries 가중**: 단순 빈도 → **최근성 우선**으로 뒤집음 (gemini 강조: "6개월 전 100회 같이 등장한 entry를 상위로 올리는 함정"). reason 필드로 노출 (codex: `co_mentioned_in_5_sessions`, `last_seen=...`)
- **session_start nudge**: directive("bundle(N0062)로 시작하세요") → 약화 (gemini: 새 작업일 때 매몰 위험). "연관 작업일 때만 탐색 시작점으로 활용" 정도
- **bundle sync_history**: 요약형 (`ts/agent/summary/sync_id`)만, 원본 record 통째로는 X. limit 5~10

### 둘이 새로 짚은 것
- `sync_record.entries` 의미를 docs에 박기 (touched로 정의). context_entries 분리는 v0.6+ 가능성으로만 열어둠 (codex)
- backward compat: invalid ID dead link 필터링 (codex), 삭제/병합된 entry graceful degradation (gemini)
- index drift 감지: validate rebuild + 차이 검출 (codex)
- agent hop loop 위험: bundle(A)→co=[B,C], bundle(B)→co=[A,D] 무한 hop. AGENTS.md에 "co_entries는 참고용, 명확한 목적 없이 연쇄 bundle 금지" (gemini)
- escalated_write 시각적 강조 태그 (`🚨 GLOBAL_WRITE_EXECUTED`) — agent가 자기 출력을 관찰하는 특성 활용 (gemini)
- safe-fail: vault 빠뜨려 local에 쓰는 건 global 오염보다 낫다. 자기 발견 가능 (gemini, codex 일치)

### 잔여 분기 (사용자 결정 미정)
v0.5.2 patch 범위:
- (a) sync_history만, co_entries는 v0.5.3 (codex 권고: 가장 안전)
- (b) co_entries 포함 + AGENTS.md 가이드 동시
- (c) co_entries 미루기 (v0.6+), sync_history만으로 한 사이클 운영

[Change] 사용자 1차 권고 → 두 모델 입력으로 3개 항목 수정 + 5개 새 항목 발견
[Impact] 잔여 분기 1개 결정 시 PLAN entry로 splitoff 가능. CI 안정화 작업으로 잠시 중단됨.

→ see N0038 (sudo 패턴 별 트랙)
→ see N0068 (CI race fix 진행)