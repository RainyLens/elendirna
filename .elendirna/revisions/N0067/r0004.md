---
baseline: N0067@r0003
created: 2026-05-18T13:50:33.295440700+09:00
---

## Delta

## v0.5.4 라이브 검증 (2026-05-18)

[Change] 본 세션이 N0067의 발현 → 업그레이드 → 검증 라이프사이클을 하나의 흐름으로 박은 사례.

### 발현 (세션 전반부, v0.5.0)
- `session_start(vault='local')` 두 번 호출 → 응답 `vault_kind="global"` 유지 (override 무력화)
- `entry_new()` 무지정 → 글로벌 vault에 N0036 생성 (사용자 의도와 어긋남)
- `entry_new(vault='local')` → "N0036 이미 존재" 오류 (글로벌 next_id 계산 후 local에 중복 ID 할당 시도)
- MCP 도구 차원 우회 경로 없음 → CLI `elf entry new` (cwd 탐색)로 local N0087에 정상 생성

### 업그레이드
- `cargo install elendirna` v0.5.0 → v0.5.4
- elf.exe 프로세스 5개 kill 후 MCP 재시작

### 검증 (세션 후반부, v0.5.4)
- `session_start(vault='local')` → 응답 `vault_kind="local"` ✓
- `vault_root="\\?\D:\Work\elendirna"` ✓
- **신규 필드 `vault_origin="cwd_search"`** — vault 결정 우선순위(--vault > --global > ELF_VAULT > cwd 탐색 > 폴백)가 응답에 명시. r0003 codex 진단의 origin 기반 가드 framework가 응답 표면까지 노출됨.

### 진단 → fix 매핑

r0003에서 제시한 fix 옵션 중 어느 것이 채택됐는지 코드 차원 미확인. 응답 표면 기준:
- (A) `CwdSearchHome` 별 origin 분리: 미확인. 본 세션은 cwd가 D:\Work\elendirna이라 home 매칭 케이스 발생 X — 추가 검증 필요
- (B) 'local' 별칭 시맨틱 재정의: 본 세션에서 override 작동했으니 일부 변경 있음
- (C) `session_start(local_path=...)` 명시 등록: 본 세션은 무지정 호출만 사용 — 미검증

→ 별 작업 후보: v0.5.1~v0.5.4 changelog 또는 코드 diff로 정확한 fix 옵션 식별. N0038 sudo 구현과의 정합 확인.

### CC Desktop 케이스 미검증

본 세션은 Claude Code(터미널 호스트). r0001 원 보고는 CC Desktop 케이스. cwd=home일 때 home 매칭 silent write 가능 여부는 별 환경에서 재현 필요.

[Impact] N0067 close 가능 단계 — 다만 (1) 어떤 fix 옵션이 채택됐는지 코드 식별, (2) CC Desktop 케이스 재현 검증, (3) N0038 sudo 정책과의 origin 가드 정합 확인이 남음. 본 revision은 *발현 → fix 사실 확인*까지의 마일스톤. 완전 close는 위 3건 처리 후.

→ see N0087 (본 세션 발산의 다른 산물 — 비강제 안내 자기-참조)
→ see N0040 (vault 출처 응답 — vault_origin 필드 신설의 직접 이행)
→ see N0038 r0014 (sudo 정책 — origin 가드 정합 확인 대상)
