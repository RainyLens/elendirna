---
baseline: N0070@r0003
created: 2026-05-11T17:25:47.170173600+09:00
---

## Delta

## Phase A 구현 완료 + 사후 검증 통과 (2026-05-11)

### 구현 적용
1. [src/vault/mod.rs:17-27](src/vault/mod.rs) — `CwdSearchHome` variant 추가 (`CwdSearch` 바로 뒤)
2. [src/vault/mod.rs:91+](src/vault/mod.rs) — `pub fn is_home_vault_root(&Path) -> bool` helper 신설. canonicalize 양쪽 성공 시에만 true, 한쪽이라도 fail 시 보수적 false
3. [src/cli/serve.rs:36-44](src/cli/serve.rs) — `find_local_vault_root` 성공 후 helper로 origin wrap
4. [src/mcp/mod.rs](src/mcp/mod.rs) `vault_meta()` — `CwdSearchHome → "cwd_search_home"` 직렬화 + vault_kind 판정에도 `is_home_vault_root` 재사용 (중복 home 비교 로직 제거)

### 테스트 (6개 신규)
- `vault::tests::home_root::{matches_actual_home, tempdir_is_not_home, nonexistent_path_is_false}` — helper 단위
- `mcp::tests::vault_meta_cwd_search_home_response_contract` — `vault_kind="global"` + `vault_origin="cwd_search_home"` 조합 고정
- `mcp::tests::vault_meta_origin_string_round_trip` — 7 variants 모두 (`Alias("x") -> "alias:x"` 포함) string round-trip 회귀 방지

`cargo test --lib`: 58 passed, 0 failed. integration 19 passed.

### codex 사후 검증 결과 — LGTM, blocker 없음
- exhaustive match로 variant 추가 누락이 compile에서 잡힘 (mcp/mod.rs vault_meta)
- `vault_kind`가 helper 기반으로 origin과 독립 — explicit home path도 `global`로 보이는 기존 의미 보존
- write guard가 `FallbackGlobal`만 보는 것은 Phase A 범위로 OK (Phase B에서 확장)

### Phase A 단독 머지 시 동작
- origin 표시는 `cwd_search_home`으로 정확히 노출
- write 차단은 아직 미동작 (Phase B 의존) — 의도된 중간 상태

[Change] Phase A 코드 + 6개 테스트 + nit(Alias variant round-trip) 보강
[Impact] silent global write 함정에 대한 origin 분리 완료. 차단은 Phase B에서. test infrastructure (CWD_LOCK + is_home_vault_root)가 Phase B 가드 테스트의 기반이 됨

→ Phase B 진입: [src/mcp/mod.rs](src/mcp/mod.rs) 7곳 guard pattern 확장 (`FallbackGlobal | CwdSearchHome`)