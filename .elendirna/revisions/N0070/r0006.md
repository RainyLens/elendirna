---
baseline: N0070@r0005
created: 2026-05-11T17:34:27.754288700+09:00
---

## Delta

## Phase B 완료 + cadence 메타 (2026-05-11)

### codex prompt 해석 메모

본 세션의 Phase B 사전 검증 prompt를 codex가 "implement 의도"로 해석해 직접 구현 + N0070 r0005 자체 기록. PLAN cadence("사전 검증 → Claude 구현 → 사후 검증")가 한 단계 압축됐지만 결과 자체는 review 가능. lesson: codex `exec` prompt에 "review only, no edits" 같은 라벨을 명시적으로 박아야 cadence 충실. 본 세션은 결과 수용으로 진행.

### Phase B 적용 결과 (codex r0005 + 본 revision)

- [src/mcp/mod.rs:87-104](src/mcp/mod.rs) `ensure_write_confirmed(&VaultResolution, bool)` helper 신설 — 7곳 중복 가드를 한 함수로 centralize. PLAN의 "각 위치에서 matches! 패턴 확장"보다 코드 표면 더 작고 errors 한 지점 집중
- 7곳 모두 `Self::ensure_write_confirmed(&res, p.confirm)?;` 한 줄로 대체 (entry_new:481 / entry_status:511 / revision_add:561 / sync_record:697 / validate:716 / entry_attach:737 / entry_detach:761)
- 7개 도구의 `confirm` schema description도 두 guarded origin 모두 언급하도록 갱신
- 에러 메시지 분기: `FallbackGlobal` → 기존 유지, `CwdSearchHome` → "writing to host-default global vault requires confirm=true — cwd is at home, vault resolved to a global location". 둘 다 `ErrorData::invalid_params`

### Integration test cadence 조정

PLAN의 "3 case integration test"는 helper 추출로 단순화됨. tool method가 not-pub이라 외부 integration 호출 불가 — 대신 [src/mcp/mod.rs:1037](src/mcp/mod.rs) `write_guard_requires_confirm_for_implicit_global_origins`가 두 guarded origin reject + confirm=true allow + 정상 5 origin 무차단을 한 test에서 cover. 7곳 호출 일관성은 코드 review로 확인.

### 검증
- `cargo test --lib`: 58 passed
- `cargo test --test mcp_integration`: 19 passed (회귀 0)
- `cargo fmt` (본 PR 4 파일만): pass
- broad fmt drift는 unrelated dirty 파일 (entry.rs 등)에 있음 — 본 PR 외 정리

[Change] Phase B 가드 확장 완료 (helper 방식). Integration test PLAN을 helper unit test로 동등 대체. codex prompt 해석 mismatch는 lesson 기록
[Impact] silent global write 함정이 7개 write 도구 모두에서 차단됨. CwdSearchHome state에서 confirm 누락 → invalid_params + 구체적 에러 메시지. v0.5.3 PR의 사용자-facing 핵심 fix 완성

→ Phase C/D/E는 별 결정. Phase A+B만으로도 N0067 보고 함정(silent global write)은 close됨. C/D/E는 응답 contract 보강 + 문서화로 nice-to-have