---
baseline: N0076@r0000
created: 2026-05-08T14:53:39.082239700+09:00
---

## Delta

[Change] 본문 작성 (라이브 세션 기록).

## 발산점

사용자 제안 — `elf init` 직후 사용자/AI 콜드스타트를 덜 cold하게 만들 가이드 entry군. AI 행동 + 사용자 행동 동시 유도.

## claude 1차 응답: 두 반박 (둘 다 retrieval 없이)

1. "CLAUDE.md에 이미 규칙이 있으니 entry는 패턴/사례용으로 분리" — 가설.
2. "elen-labs 분리 정책상 가이드 entry가 핵심인지 add-on인지 판단 필요" — 가설.

→ 둘 다 vault entry 한 번 query 없이 던진 claim.

## 사용자 재반박 + 사실 확인

> "elf 도구 자체가 그렇게 md파일을 명시적으로 쓰지 않아요, 한번 찾아보세요."
> "elen-labs는 플러그인과 관계 없는 반박 같습니다."

→ see N0007 — `elf init`이 박는 CLAUDE.md는 3줄 violation 가드뿐. 풍부한 본문은 도그푸딩 잔재.
→ see N0063 — Guide entry는 vault 본체에 이미 거주.

claude 두 claim 모두 무너짐.

## claude 1차 self-analysis

- 1번: 도그푸딩 vault의 살찐 CLAUDE.md를 "elf 표준 상태"로 무의식 매핑 (자기-위치 오인).
- 2번: 1번 진단 위에 같은 형태로 끼워맞춘 과적합. 실제 원인은 N0063 데이터 미확인.
- 결론: AI 잘못이라기보다 환경 부족. 도구로 막아낼 영역.

## 사용자 지적

"2번에 환각이 좀 섞였다." 1번/2번을 별개 함정으로 진단한 self-analysis 자체도 정확하지 않음 → 사용자 통찰: "두 번 실수했으면 패턴일 가능성".

## 비강제 nudge 후보 (a)/(b)/(c) 발산

→ see N0050 (non-compulsive AI guidance) 정신을 도구 자체에 적용:

- (a) `session_start`/`query` 응답 hint 슬롯에 frame 태그 reference
- (b) `frame` 태그 anchor entry + `query(tag='frame')` 습관
- (c) `sync_record` 시점 micro self-check

## codex 검증 (외부 모델 호출)

`codex exec --full-auto` (model: gpt-5.5, MCP=elendirna 연결됨). N0075 + N0050/N0007/N0063 bundle 후 평가:

- **두 오판은 별개 함정이 아니라 한 패턴**: *"retrieval 없이 규범/범위 claim 먼저 만든 뒤 설계 언어로 봉합"*. claude self-analysis보다 압축적·정확.
- 도구 약점: `bundle(depth=1)`이 본문의 `→ see N####`를 linked로 미확장.
- "표준 init 상태", "핵심/add-on 경계", "이미 가이드가 있다" 같은 claim에 entry id가 자연스럽게 붙어야 함.
- **self-check 루틴보다 provenance hint가 낫다.**
- 후보 재정의: (a) *source-bearing* hint / (b') tool 응답이 optional frame anchors 노출 / (c) low priority.
- 누락 후보: **scope/provenance guard** — 정책/경계 claim 시 관련 entry id + 확인 상태 노출 UX.

기록 → see N0075 r0002.

부수 관찰:
- codex가 직접 호출한 `revision_add` + `sync_record` 두 건이 *user cancelled*로 종료. 사용자는 제어한 적 없음. 원인 미상.
- codex 세션 마지막 *"failed to record rollout items: thread 019e029a-... not found"* 에러 — 관련성 미확인.

## 메타 사건: 진단 직후 같은 패턴 즉시 재현 (3회째)

codex revision_add cancel 원인에 대해 claude가 가설 제시:

> "최근 커밋의 N0070 PLAN: vault sudo 가드... 가 만약 작동했다면 codex의 revision_add/sync_record가 둘 다 차단된 게 자연스럽게 설명됩니다."

→ N0070 bundle 0회. commit message의 *"vault sudo 가드"* 단어 매칭만으로 인과 서사 구축.

사용자 후속 질문 ("이 건에 대한 요약이 N0070으로 간주해도 될까요?")로 retrieval 트리거 → see N0070 bundle 결과:

- N0070이 가드하는 시나리오: home에 글로벌 vault + process cwd=home + silent global write
- 이번 호출 시나리오: workdir=`D:\Work\elendirna` 명시적 local vault
- 두 시나리오 무관
- N0070은 draft PLAN, **미구현**

claude의 N0070 가설 무너짐.

## 의미

이 세션은 codex가 명명한 *"retrieval 없이 규범/범위 claim → 설계 언어 봉합"* 패턴의 **라이브 confirmation**:
- 1회: CLAUDE.md vs entry 분담 (N0007 미확인)
- 2회: elen-labs 분리 정책 적용 (N0063 미확인)
- 3회: N0070 vault sudo 가드 (N0070 미확인) — codex 진단 받은 직후

3회 모두 동일 구조: vault에 fact가 있는데 retrieval 생략 → 그럴듯한 설계 언어로 가설을 claim화. 진단 받은 직후에도 즉시 재발한 게 핵심 — self-check 루틴이 막을 수 없는 자동성을 가진 패턴이며, codex 권고대로 **provenance hint 우선순위 상향**의 살아있는 정당성.

## 미해결

- codex revision_add cancel 실제 원인 (codex 세션 로그 `019e029a-ffe0-7cb3-803a-3047cbab8499` 직접 추적 필요)
- (a) source-bearing hint / (b') tool-surfaced anchors / scope-provenance guard 격상 시점 — N0075에서 묵힘 중

[Impact] retrieval-before-claim 패턴의 case-study 자료. 추후 provenance hint 도구 디자인 PR 작성 시 근거 raw data. AI self-analysis가 같은 패턴의 다른 형태로 빠질 수 있다는 한계도 같이 박힘.

→ see N0075 (seed entry, codex 검증 결과 누적)
→ see N0050 (non-compulsive AI guidance — 후보 발산 기준)
→ see N0007, N0063, N0070 (claude가 retrieval 생략한 3회 사례의 reference)