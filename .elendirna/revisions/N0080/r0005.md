---
baseline: N0080@r0004
created: 2026-05-20T14:52:40.455040+09:00
---

## Delta

[Change] v0.6.0 PR(N0091)에 포함되어 결정 묵힘 종료 (2026-05-20).

사용자 결정: **tag 변경 자유롭게, 정책 제한은 미래**. r0001~r0004에서 정리된 "tool-mediated mutation" 원칙에 따라 `elf entry tag add/remove/set` 도구 신설.

구현 spec (N0091 r0002 Phase H):
- CLI: `elf entry tag add <id> <tag>`, `tag remove <id> <tag>`, `tag set <id> <tag1,tag2,...>`
- MCP: `entry_tag_add(id, tag)`, `entry_tag_remove(id, tag)`, `entry_tag_set(id, tags[])`
- manifest 직접 write (atomic_write)
- sync event 기록 (`tag.added`, `tag.removed`, `tag.set` — 일관성)
- 정책 제한 (audit, immutable check, anti-taxonomy 가드 등) 미래
- title/status는 본 PR scope 외 (다음 cycle)

[Impact] CRUD U 결손 첫 해소 (tag 한정). manifest mutability layer 정식 진입. title/status/links 등 다른 mutable field는 후속 PR에서 점진적.

→ see N0091 r0002 (흡수 + Phase H에서 구현)
→ see N0050 (Non-Compulsive AI Guidance — AI 비대칭 soft steering 원칙)