---
baseline: N0081@r0003
created: 2026-05-12T11:35:54.214888900+09:00
---

## Delta

## v0.5.4 publish 완료 (2026-05-12)

### 배포 결과
- **crates.io**: `elendirna v0.5.4` published (Uploaded + 동기화 확인)
- **upstream**: `90a6369` release squash commit (8b538a6..90a6369 ff push)
- **workbench**: `cedb150` (release 메타 sync — Cargo.toml 0.5.4 + CHANGELOG entry)

### publish 흐름에서 배운 점 (workflow 정정)

표준 순서가 뒤집힘 — publish 브랜치에 release 메타 먼저 박고 그 다음 workbench main으로 sync하는 흐름으로 진행됐는데, 사용자 멘탈 모델은 workbench가 항상 release state까지 포함한 truth source이고 publish 분기는 그 시점에 vault만 빼서 분기. 즉:

1. workbench main에 Cargo.toml bump + CHANGELOG entry 먼저
2. publish 브랜치 분기 (vault 제외)
3. upstream push + `cargo publish`

→ auto-memory에 `feedback_release_workflow.md`로 저장.

### 응답 contract 최종 (v0.5.4)
- `vault` / `vault_kind` / `vault_origin` — 모든 응답에 항상
- `fallback: true` — `fallback_global` origin일 때만
- `escalated_write: true` + `warning: "🚨 GLOBAL_WRITE_EXECUTED — ..."` — guarded origin(`FallbackGlobal | CwdSearchHome`) + `confirm=true` 통과 시에만

### v0.6+ OUT 영역 (별 PLAN 후보)
- `vault_origin` → `vault_resolution` 필드명 변경 (semantic 정확화, major bump)
- `session_local` / `primary` origin enum 확장
- destructive/bulk confirm 가드 (validate / detach / bulk migration) — 축 다름

[Change] v0.5.4 publish 완료 marker. release workflow 순서 정정 lesson 기록
[Impact] N0081 PLAN의 모든 phase(A/B/C/D/E) close. v0.6+ 새 PLAN 시작 가능 상태