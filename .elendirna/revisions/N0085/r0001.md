---
baseline: N0085@r0000
created: 2026-05-15T10:16:28.651931400+09:00
---

## Delta

[Change]
entry_list 응답이 현재 size 정보 미포함. 진행 계획이 있었던 것으로 회상되지만 vault 기록 없음 (2026-05-15 query 시점 확인).

## Goal

entry_list 응답 JSON에 entry별 size hint 포함. agent가 bundle 호출 전 cost 예측 가능.

## Required changes (rough)

- entry_list handler (`src/mcp/mod.rs` 또는 vault ops) — 응답 schema에 `size_bytes` 또는 `note_bytes` 필드 추가
- 데이터 소스: sqlite 인덱스에 이미 size 있으면 그대로 사용, 없으면 note.md stat
- 후방 호환: 추가 필드라 break X

## Acceptance

- entry_list 호출 시 각 entry의 byte 크기 응답에 포함
- bundle 호출 전 agent가 "이 entry 크다 / 작다" 판단 가능

## Cross-link

- Global N0034 (cost telemetry design philosophy)
- Local N0086 (Default cost-aware parameters on bundle) — 본 작업이 전제 조건

## Why now

ELF 비교 분석 (Drift_Response_Comparison.md, 2026-05-14)에서 양측 Cost Feedback 🔴 약점 surface. 이 작은 변경으로 entry-level cost visibility 한 단계 향상.

[Impact]
agent-side cost telemetry 1차 신호. cost-aware bundle 호출 합리적 default의 전제 조건.