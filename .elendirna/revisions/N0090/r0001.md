---
baseline: N0090@r0000
created: 2026-05-20T11:39:03.976335500+09:00
---

## Delta

## Goal

N0089 r0002 spec 구현. `Fallback` init에서 기존 vault 채택 + warning, `Explicit` init은 기존 동작 보존. v0.5.4까지 적용된 origin 가드 layer(N0070+N0081)와 정합 유지.

baseline: N0089 (splitoff). 의존: N0070/N0081 머지된 main.

## 함정 위치 (코드 실측)

`src/cli/serve.rs:45-66`의 `Err(ElfError::NotAVault)` 분기:
1. home 결정 → stderr "[elf] vault 없음 — 글로벌 vault 자동 생성: {home}"
2. `crate::cli::init::run(InitArgs { path: home, global: true, .. })?` — **`?`가 모든 함정의 원천**
3. `init::run`은 `src/cli/init.rs:83-87`에서 `config.toml` 존재 시 `ElfError::AlreadyInitialized` 반환
4. `?`로 위로 전파 → `serve::run` Err → process exit 비정상 → Desktop transport close → re-spawn 무한 루프

fix 진입점: `init.rs` 한 군데(L83-87 분기 + 시그니처) + `serve.rs` 한 줄(L56 호출) + `mod.rs` CLI 서브커맨드 한 줄.

## Scope

### IN

#### Phase A: `InitContext` enum + `init::run` 시그니처
- `src/cli/init.rs`: `pub enum InitContext { Explicit, Fallback }` 신설
- `pub fn run(args: InitArgs, ctx: InitContext) -> Result<(), ElfError>` 시그니처 변경
- L83-87 분기:
  - `Explicit` → 기존 `Err(AlreadyInitialized { path })` 보존
  - `Fallback` → stderr warning("기존 vault가 있으니 내용 섞임에 유의: {path}") + `Ok(())` 조기 반환 (호출자가 path 이미 알고 있음 → side effect 없이 종료)

#### Phase B: 호출자 분기
- `src/cli/mod.rs`: CLI `init` 서브커맨드 dispatch → `InitContext::Explicit` 전달
- `src/cli/serve.rs:56`: `crate::cli::init::run(args, InitContext::Fallback)?` — `?` 유지하되 Fallback 분기가 Ok 반환하므로 더 이상 process suicide 안 함
- serve.rs는 fallback 경로 끝(L62-65)에서 그대로 `VaultResolution { path: home, origin: FallbackGlobal }` 사용

#### Phase C: 사후 검증
- `cargo test --lib` / `cargo test --test mcp_integration` 회귀 0
- codex 사후 review (N0070 r0004 패턴) — 결정안: blocker 없으면 진행

### OUT (별 라인 아이템)

- **CLI 다른 명령 fallback path 감사** — `elf entry new` 등이 vault detection 실패 시 같은 fallback path 트리거 가능성. InitContext 전파 누락 여부 audit. 별 PR 권장 (스코프 큼, 회귀 위험, N0089 r0002 사용자 결정 시점에 별 라인으로 명시)
- **N0067 close → archived** — Phase A+B 머지 후 N0088 정책 따라 별도 적용. 본 PR(N0090) 머지 완료 시 r0007/r0008로 close marker
- **session_start 응답 warning 필드에 init 시점 메시지 동봉** — v0.5.4 `vault_meta`의 기존 `fallback:true` + `warning` layer와 평행. 결정: 별 새 필드 X, 기존 warning 메시지에 init context 사실 append 검토. 코드 진입 단계에서 결정(현재 PLAN의 약한 항목 — 구현 시 응답 contract 확정)
- **`mark_escalated_if_needed` layer와의 추가 정합** — Fallback init이 채택한 vault에 write 시 기존 v0.5.4 escalated_write 자동 작동. 별 hook 불필요. 본 PR 변경 없음

## 리스크 / 호환성

### `init::run` 시그니처 변경
- 외부 caller: `src/cli/mod.rs` (CLI dispatch) + `src/cli/serve.rs` (fallback)뿐. internal API
- 사용자-facing CLI 동작: 무변화 (`Explicit` 분기로 기존 보존)
- 회귀 위험: 낮음

### Desktop 사용자 영향
- 이전: args 무지정 + home에 기존 vault → 무한 루프
- 이후: 정상 기동 + stderr warning + `vault_origin="fallback_global"` 응답
- N0089 r0001 적용된 워크어라운드(`--global` 추가)는 그대로 작동 (more explicit이므로 변경 없음)

### N0038 sudo 정책 layer 정합
- N0089 layer = process 기동 시점 1회 init 가드
- N0070/N0081 layer = call 시점 write 가드 (`ensure_write_confirmed` + `escalated_write`)
- 두 layer 직교, 충돌 없음
- 조합: Fallback init이 채택한 vault → origin `FallbackGlobal` → 이후 write call에서 자동 가드 + escalated_write 응답 → 다중 안전망 ✓

### Warning 의미 분리
- 기존 v0.5.4 `vault_meta` warning = "primary 못 찾아 fallback 잡힘" (call 시점)
- 신규 init warning = "fallback path에서 vault 있어 채택, 섞임 주의" (init 시점 1회)
- 의미 중복 없음

## 테스트

### 단위 (`src/cli/init.rs`)
- `init_explicit_existing_vault_returns_already_initialized`: 기존 vault + `Explicit` → `AlreadyInitialized` (회귀 보장)
- `init_fallback_existing_vault_returns_ok`: 기존 vault + `Fallback` → `Ok(())`, stderr 검증(가능하면)
- `init_fallback_new_path_creates_vault`: vault 없음 + `Fallback` → `Ok(())` + 정상 생성

### Integration (`tests/`)
- serve 진입점이 vault 아닌 cwd + home에 기존 vault 상태에서 process 살아있는지 + `vault_origin="fallback_global"` 응답 검증
- 가능하면 process suicide 회귀 방지 test로 박음

## 일정 / release

- v0.5.5 patch. 코드 변경 표면 작음(파일 3개, 함수 1개 시그니처 변경 + 호출 2곳 + enum 1개)
- 사전 검증 → 구현 → 사후 검증 cadence (N0070 r0003~r0007 모델)

## 의존성

- N0089 r0002 spec (사용자 결정 완료)
- N0070/N0081 머지된 main 브랜치 (v0.5.4까지 origin 가드 layer 완성된 상태)

## 성공 기준

- Desktop 환경에서 args 무지정 + home에 기존 vault → 무한 루프 없이 정상 기동
- 기존 `elf init` CLI 동작 회귀 0
- `cargo test --lib` / `cargo test --test mcp_integration` pass
- codex 사후 review blocker 0

[Change] N0089 r0002 spec을 v0.5.5 PR PLAN으로 splitoff. 함정 위치 코드 실측으로 확정, Phase A/B/C 분할
[Impact] 코드 fix 진입 준비 완료. 다음 단계 = codex 사전 검증(N0070 r0003 패턴) → 구현 → 사후 검증

→ see N0089 r0002 (spec 본체)
→ see N0070 (v0.5.3 PLAN — Phase A+B 모델, sudo 가드 layer)
→ see N0081 (v0.5.4 PLAN — Phase C/D/E 모델, escalated_write 응답 contract)
→ see N0067 r0006 (변종 cover marker — 본 PR 머지 시 r0007/r0008로 close)