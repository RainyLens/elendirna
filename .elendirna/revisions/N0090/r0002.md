---
baseline: N0090@r0001
created: 2026-05-20T11:49:46.175532800+09:00
---

## Delta

## codex 사전 검증 결과 (2026-05-20, Phase A 시작 전, Claude+codex 협업)

`codex exec --dangerously-bypass-approvals-and-sandbox -C D:/Work/elendirna` + elendirna MCP로 N0090/N0089/N0067/N0038/N0070/N0081 본문 직접 흡수 후 코드 실측. **Blocker 0**. Medium 4 / Low 2. PLAN 수정 권고 4건.

### High — 없음

`Explicit`/`Fallback` init context 분리는 process suicide 원인과 직접 맞고, v0.5.3/v0.5.4 origin guard layer와 충돌 없음.

### Medium

**1. PLAN의 "호출자 2곳" 판단은 맞지만 파일 reference 오류**
- 실제 CLI dispatch는 `src/cli/mod.rs`가 아니라 [src/main.rs:77-78](src/main.rs). cli/mod.rs는 module declaration만 (L1-12)
- `init::run` 실제 production caller는 [src/main.rs:78](src/main.rs) + [src/cli/serve.rs:56](src/cli/serve.rs) 두 곳
- 시그니처 변경 자체가 compile error로 누락 caller를 잡으므로 blocker는 아니나, PLAN reference 정정 필수
- **PLAN 수정안**: Phase B의 `src/cli/mod.rs` → `src/main.rs:78`로 교체

**2. `init::run(args, ctx)` 시그니처는 적절하나 public API break 명시 필요**
- `InitArgs`는 clap 파싱 구조체 [src/cli/init.rs:49-66](src/cli/init.rs). ctx 필드 추가는 CLI 인자 surface와 내부 호출 의도가 섞임 → `run(args, ctx)` 별 파라미터가 더 낫다 (PLAN 결정 유지)
- 단, `cli` 모듈이 library public surface로 열려 있음 [src/lib.rs:1](src/lib.rs). 현재 tests도 `elendirna::cli::init::run`을 직접 사용: [tests/mcp_integration.rs:7](tests/mcp_integration.rs), [tests/sqlite_integration.rs:6](tests/sqlite_integration.rs), [tests/integration.rs:6](tests/integration.rs)
- pre-1.0이라 감수 가능하나, PLAN의 "internal API라 risk 낮음"은 약간 과장
- **PLAN 선택지**:
  - (a) 단순하게 시그니처 변경 후 tests 전부 `Explicit`로 갱신
  - (b) public 호환 유지: `pub fn run(args)`는 `Explicit` wrapper로 유지, `pub(crate) fn run_with_context(args, ctx)`를 serve에서 호출

**3. stderr warning만으로는 MCP contract 관점에서 약함**
- 함정 지점 확정: [src/cli/serve.rs:45-66](src/cli/serve.rs)에서 [src/cli/init.rs:83-87](src/cli/init.rs)의 `AlreadyInitialized`가 `?`로 전파 ✓
- stderr는 Desktop host 로그로 남지만 agent 응답 contract에는 보장 X
- 현재 `vault_meta`는 `FallbackGlobal`에 대해 `fallback:true`만 inject, 일반 warning은 inject 안 함 [src/mcp/mod.rs:61-87](src/mcp/mod.rs). `session_start`가 이 meta를 그대로 사용 [src/mcp/mod.rs:925, 945-949, 980-983](src/mcp/mod.rs)
- `warning` 필드는 이미 write escalation에서 사용 [src/mcp/mod.rs:112-132](src/mcp/mod.rs), attach 결과에도 operation-level warning 존재 [src/mcp/mod.rs:827-839](src/mcp/mod.rs)
- **PLAN 수정안**: process suicide fix 자체는 stderr만으로 충분. N0089 "내용 섞임에 유의" phrasing을 agent에 노출하려면 `session_start`에만 warning 붙이거나, generic `vault_meta.warning` 대신 새 `vault_warning` 또는 `warnings[]` 필드를 신설하는 쪽이 기존 escalated_write warning과 충돌 적음. PLAN Phase C(현재 "약한 항목")를 이 결정으로 박을 것

**4. process suicide 회귀 test는 현재 test 형태로는 부족**
- 현재 `tests/mcp_integration.rs`는 실제 MCP process가 아니라 `vault::ops` 직접 호출 위주 [tests/mcp_integration.rs:1-10](tests/mcp_integration.rs). `assert_cmd`는 이미 dev-dependency에 있음 [Cargo.toml:45-48](Cargo.toml)
- PLAN의 unit 3개는 `init::run` 분기 보장에는 충분하나, "Desktop transport close → re-spawn loop" 회귀는 process-level test가 더 정확
- **PLAN 수정안**: 새 integration test는 `elf serve --mcp`를 non-vault cwd에서 spawn + temp `USERPROFILE`/`HOME`에 기존 vault 둔 뒤 짧은 시간 안에 process가 exit하지 않는지 `try_wait()`로 확인 후 kill. PLAN Phase 테스트 섹션의 "process suicide 회귀 방지 test를 명시적으로 박을 방법이 있는가" 질문이 이 패턴으로 해결

### Low

**1. 다른 CLI 명령이 init을 트리거할 가능성은 코드상 낮음**
- `elf entry new` 등 일반 CLI 명령은 `init::run`을 부르지 않고 `resolve_vault_root`만 탐 [src/cli/entry.rs:59](src/cli/entry.rs), [src/vault/mod.rs:52-75](src/vault/mod.rs). 탐색 실패 후 기존 home vault 채택 fallback은 [src/vault/mod.rs:130-150](src/vault/mod.rs)에 있지만 새 vault auto-init은 하지 않음
- `serve`만 `find_local_vault_root` 실패 시 `init::run`을 호출 [src/cli/serve.rs:36-66](src/cli/serve.rs)
- → PLAN이 "CLI 다른 명령 fallback path 감사"를 OUT으로 둔 판단 유지 가능. 다만 CLI 일반 명령의 implicit global fallback write 자체는 별 정책 이슈로 잔존

**2. origin guard layer 다중 안전망 가설 코드 실측 성립 확인**
- Fallback branch가 최종적으로 `VaultOrigin::FallbackGlobal` 부착 [src/cli/serve.rs:62-65](src/cli/serve.rs)
- MCP server가 launch resolution 보존 [src/mcp/mod.rs:48-55](src/mcp/mod.rs), 기본 tool vault resolve도 origin 유지 [src/mcp/mod.rs:181-199](src/mcp/mod.rs)
- write guard가 `FallbackGlobal | CwdSearchHome`에 대해 `confirm=true` 요구 [src/mcp/mod.rs:90-107](src/mcp/mod.rs). 통과 후 `escalated_write:true` + `warning` 부착 [src/mcp/mod.rs:112-132](src/mcp/mod.rs)
- 주요 write call site도 guard 후 실행 순서 [src/mcp/mod.rs:540-559, 575-616, 629-643, 775-790](src/mcp/mod.rs)
- → r0001 가설(Fallback init이 채택한 vault에 대한 이후 write가 자동 가드 + escalated_write로 다중 안전망) 코드 실측 성립

### PLAN 반영 항목 (구현 진입 전 r0003에서 정리 권장)

1. Phase B reference: `src/cli/mod.rs` → `src/main.rs:78`
2. Phase A 시그니처 design: 선택지 (a) 단순 갱신 vs (b) wrapper 보존 — 사용자 결정 필요
3. Phase C warning contract: 새 필드(`vault_warning` 또는 `warnings[]`) vs `session_start` 한정 — 사용자 결정 필요
4. 테스트 섹션에 process-level regression integration test 추가 (`assert_cmd` 기반, `try_wait()` 패턴)

[Change] codex 사전 검증 통합. Medium 4 / Low 2 / Blocker 0. PLAN 수정 권고 4건. cli/mod.rs reference 오류 발견, public API surface 판단, warning contract layer 결정 필요, process-level regression test 보강
[Impact] PLAN scope는 유지(여전히 작음). 구현 진입 전 r0003에서 사용자 결정 2건(시그니처 design, warning contract)을 확정한 후 진입. codex 사후 검증은 N0070 r0004 패턴으로 구현 후 별 라운드

→ see N0070 r0003 (codex 사전 검증 모델 — 본 r과 동일 cadence)
→ see N0070 r0006 (codex prompt 해석 lesson — 본 r은 "review only, no edits" 라벨 명시로 cadence 충실)
→ codex 협업 sync: codex가 자체 sync_record를 sync.jsonl에 남길 가능성 있음 (협업 흔적, 정책상 허용)