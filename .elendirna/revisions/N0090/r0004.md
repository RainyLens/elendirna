---
baseline: N0090@r0003
created: 2026-05-20T14:18:12.992057800+09:00
---

## Delta

## A 채택 확정 + B splitoff (2026-05-20)

r0003의 결정 2-sub(통합/공존) + warning phrasing(이모지/prefix) + level 시스템 분석을 사용자 신호로 정리.

### 사용자 결정 확정

1. **결정 2-sub: (3a) 통합** — `warnings[]` array로 escalated_write 메시지 흡수 (기존 `warning` 필드 사라짐)
2. **메시지 phrasing**: 일반 텍스트, prefix 없음, 이모지 없음 (사용자 신호 누적 — "🚨 굳이 넣을 이유 없음" + "warning: prefix 별로 — 이미 warnings[]라면 생략 가능")
3. **응답 구조**: 옵션 **A (string array)** 채택 — `warnings: Vec<String>`. level 시스템은 본 PR scope에서 제외
4. **다음 작업: B splitoff** — level 시스템(`messages: [{level, kind, message}]`) refactor는 별 PR로. **N0091** 신규 생성됨

### Phase C 최종 spec (사용자 결정 반영)

- vault_meta에 `warnings: Vec<String>` 필드 추가 (조건부 inject: warning 메시지가 있을 때만)
- 메시지 1 (escalated_write, 기존 `warning` 필드에서 이전):
  - `"confirm=true 통과로 host-default global vault에 쓰기가 실행되었습니다."`
  - inject 조건: `mark_escalated_if_needed` 동일 (guarded origin + confirm=true)
- 메시지 2 (init_context_fallback, N0089 phrasing):
  - `"기존 vault가 있으니 내용 섞임에 유의: {path}"`
  - inject 조건: launch 시점에 `InitContext::Fallback`으로 init이 진행됐고 채택한 vault가 기존에 있던 경우. `ElfMcpServer`에 `launch_init_fallback: bool` 필드 추가하여 launch 시점 사실 보존
- 기존 `warning` (string) 필드 제거 — v0.5.4 contract break, [CHANGELOG.md](CHANGELOG.md) 명시 필요
- 7개 write tool 응답의 `mark_escalated_if_needed` 로직: `warning` 단일 string inject → `warnings[]` array에 push로 변경

### Cons-2 framing 재진단 (사용자 인사이트)

> "사실 2는 B로 가면 없어질 내용이긴 한데."

framing 문제임을 확인. Elendirna error는 JSON-RPC protocol error로 별 channel 처리(`ErrorData`) — 응답 본문 messages[]에 error 안 들어오는 것은 dormant가 아니라 layer 분리의 자연 결과. B PLAN(N0091)에서 spec 정의로 흡수.

### Cons-3 대체안 (N0091로 이관)

info 경계 모호 문제는 B PR 진입 시점 결정. 옵션 (i) sub-category / (iv) info 필드 보존 / (v) notice level 추가 — N0091 r0001에 정리

### Phase 최종

- **Phase A**: `InitContext { Explicit, Fallback }` enum + `pub fn run(args)` wrapper + `pub(crate) fn run_with_context(args, ctx)` 신설 + [init.rs:83-87](src/cli/init.rs) ctx 기반 분기
- **Phase B**: [serve.rs:56](src/cli/serve.rs) `run_with_context(.., Fallback)`로 변경 (1줄)
- **Phase C**: vault_meta에 `warnings: Vec<String>` 신설. launch_init_fallback 보존 메커니즘. N0089 phrasing inject. 기존 `warning` 필드 제거 + 7개 write tool inject 로직 `warnings[]` push로 변경. 메시지 이모지/prefix 모두 제거
- **Phase D**: process-level regression integration test (`assert_cmd` + `try_wait()`)

### 잔여 결정 (구현 진입 직전)

없음. 구현 진입 가능 상태. 다음 cadence: Phase A 구현 → Phase B → Phase C → Phase D → codex 사후 review (N0070 r0004 패턴)

### codex 사후 review prompt cadence

PLAN cadence(N0070 r0006 lesson): "review only, no edits" 라벨 명시. 단 구현 후 사후 review는 코드 review를 명시적으로 요청하므로 다른 라벨 — "implementation review, no further edits unless blocker found".

[Change] 사용자 결정 4건 확정(통합/일반텍스트/A채택/B splitoff). N0091 신규 생성으로 B 분리. Phase C 최종 spec(`warnings: Vec<String>`, 이모지/prefix 없음, 기존 `warning` 필드 제거) 박음. 구현 진입 잔여 결정 0
[Impact] v0.5.5 코드 변경 표면 확정. Phase A/B/C/D 진입 가능. backward compat: 기존 `warning` (string) 필드 제거가 v0.5.4 contract break — changelog 명시 필요

→ see N0091 (B splitoff — level 시스템 정식 refactor, v0.6.0 후보)
→ see N0040 r0004 (vault_meta 응답 contract layer — warnings[] 추가가 본 layer 첫 확장)
→ see N0089 r0002 (warning phrasing 본문 — 이모지/prefix 없는 자연어 stance 정합)