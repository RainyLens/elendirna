---
baseline: N0091@r0001
created: 2026-05-20T14:52:21.843652600+09:00
---

## Delta

## v0.6.0 main PLAN 격상 — N0090/N0086/N0080/N0067 흡수 (2026-05-20)

사용자 결정에 따라 본 entry를 v0.6.0 PR의 main PLAN으로 격상. N0090(v0.5.5 → v0.6.0 격상)을 비롯해 cost-telemetry/schema-mutability 안건들을 본 PR cycle에 흡수.

### 흡수 entry chain

- **N0090 v0.5.5 PLAN** (init context 분리) → 본 entry로 흡수. v0.6.0 PR scope의 일부 (Phase A/B). N0090 prod 변경(`InitContext`/wrapper/serve.rs)은 그대로 살아 본 PR에 포함. N0090 자체는 PR 머지 시 archived
- **N0086** (bundle default cost-aware params) → 본 PR scope. 사용자 결정 "default cap 적용" (구체값 결정 raise 예정)
- **N0080** (manifest mutability + 태그 편집 결손) → 본 PR scope. 사용자 결정 "tag 변경 자유롭게, 정책 제한은 미래". `elf entry tag add/remove/set` CLI + MCP 추가
- **N0067** (per-call vault override 잔여) → 본 PR cleanup marker로 close + archived. 잔여 3건 모두 본 세션에서 처리 완료된 상태 확인 (r0006 변종 cover + CHANGELOG fix 옵션 매핑 + N0038 sudo 정합 코드 실측)

### v0.6.0 PR scope 최종

#### Logging/Messaging layer (본 PR main)
- N0091 messages[] 통합 — `Message { level, kind, message }` 구조체 + vault_meta에 `messages: Vec<Message>`
- Logging 도메인 통합: vault_meta init_fallback / mark_escalated_if_needed escalated_write / validate warnings/errors / entry_attach collision warning 모두 messages[] format
- (이전 r0001의 Cons-3 결정) info성 필드(`hint`, `next_action`, `context_hints`, `handover_status`)는 (iv) 보존 — info 통합은 v0.6.1+

#### N0040 vault_meta layer (한 묶음)
- 응답 출처 표시 정책의 정식 확장으로 messages[] 채택

#### N0090 init context (Phase A/B 이미 prod 완료)
- `InitContext { Explicit, Fallback }` + wrapper + serve.rs (이미 박힘)
- vault_meta에 init_fallback warning은 messages[]에 `{level:warning, kind:init_context_fallback, ...}` 형태로

#### Cost-telemetry (N0086)
- bundle 응답에 default cap 적용
- 구체값 결정 raise 예정 (옵션 a/b/c)

#### Schema mutability (N0080)
- `elf entry tag add <id> <tag>` / `tag remove <id> <tag>` / `tag set <id> <tag1,...>` CLI
- MCP 대응 (`entry_tag_add`, `entry_tag_remove`, `entry_tag_set`)
- 정책 제한(audit/immutable check 등) 미래
- title/status는 본 PR scope 외

### Phase 재구성

| Phase | 작업 | 자율/검토 | 상태 |
|---|---|---|---|
| A | InitContext + wrapper + serve.rs (N0090) | 자율 | 완료 |
| B (이전 C) | launch_init_fallback + vault_meta instance method (N0090 → N0091 흡수) | 자율 | 완료 |
| C | `Message` 구조체 + vault_meta `messages[]` migrate | 자율 | 진행 예정 |
| D | mark_escalated_if_needed → messages[] push | 자율 | 진행 예정 |
| E | validate 응답 messages[] migrate + warnings count 분리 | 자율 | 진행 예정 |
| F | entry_attach 응답 messages[] migrate | 자율 | 진행 예정 |
| G | bundle default cost cap (N0086) | 자율 (구체값 결정 후) | 진행 예정 |
| H | `entry tag add/remove/set` CLI + MCP (N0080) | 자율 | 진행 예정 |
| I | session_start hint phrasing 정합 (info 보존 + messages[] 공존) | 자율 | 진행 예정 |
| J | Test 전반 update (mechanical + spec) | **검토** | 대기 |
| K | Phase D regression test (assert_cmd + try_wait + fake home + ELF_VAULT remove) | **검토** | 대기 |
| L | codex 사후 review (review-only) | review-only | 대기 |
| M | 작업 완료 보고 (PR 생략, 머지 전 사용자 보고) | 보고 | 대기 |

### Cleanup (PR 머지 시)

- N0090 archived
- N0089 archived  
- N0067 archived (N0088 정책 첫 적용)
- N0086 status 변경 (open question → resolved/archived)
- N0080 status 유지 (tag 외 axis 후속 개선 여지)

### v0.6.1 후보 (별 PR)

- N0073 비-string 파라미터 역직렬화 (systemic)

### Cons 재평가

- **Cons-2 (error level dormant)**: r0001 framing 그대로 유지. error는 별 channel(JSON-RPC), messages[] error는 future critical state 보고용 reserve
- **Cons-3 (info 경계)**: (iv) info성 필드 보존 채택. info messages[] 통합은 v0.6.1+ 묵힘

### 잔여 결정 (구현 진입 직전)

1. **N0086 default cap 구체값** — 옵션 (a) depth=0 default / (b) bytes 임계 auto-truncate / (c) since 자동 bounding 중 결정 (또는 hybrid). 사용자 raise 예정

### 작업 완료 보고 cadence

PR 생략 (1인 소유 repo, memory 정책 정합). 머지 전 작업 완료 보고 필수. 머지 후 push.

[Change] v0.6.0 main PLAN 격상. N0090/N0086/N0080/N0067 흡수. Phase 재구성. Cons-2/3 framing 확정. 잔여 결정 1건 (N0086 구체값)
[Impact] 본 PR scope 확정. 구현 진입 가능 상태 (N0086 구체값 결정 후). PR 머지 시 5개 entry 정리 (N0090/N0089/N0067 archived + N0086 status 변경 + N0080 status 유지)

→ see N0090 (흡수됨, PR 머지 시 archived)
→ see N0086 (cost cap 결정)
→ see N0080 (tag mutability 진행)
→ see N0067 (잔여 close)
→ see N0088 (entry life cycle 정책 — archived 처리 시 적용)