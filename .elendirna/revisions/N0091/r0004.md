---
baseline: N0091@r0003
created: 2026-05-20T15:40:19.519989100+09:00
---

## Delta

## codex 사후 review 결과 + 작업 완료 marker (2026-05-20)

### codex 사후 review (Blocker 0, 코드 기준)

**High** (모두 git 추적 blocker — 코드 자체는 OK)
- H-1: `src/output/message.rs` 미추적 → 본 cycle commit에 포함
- H-2: `tests/serve_process_regression.rs` 미추적 → 본 cycle commit에 포함

**Medium** (후속 PR 권장)
- M-1: CHANGELOG v0.6.0 항목 → 본 cycle에서 작성 완료 ([CHANGELOG.md](CHANGELOG.md) 신규 [0.6.0] 항목)
- M-2: vault_meta init_fallback inject가 path 기반 — explicit `vault='global'`이 같은 home vault 가리키면 부착 가능. alias 경계 정리는 v0.6.1 후보
- M-3: process regression test는 liveness만 — MCP initialize 응답 검증까지는 후속 보강

**Low** (모두 OK 확인)
- spec/구현 정합 (InitContext + wrapper, serve fallback) ✓
- messages[] contract 흐름 일관성 (push_message append, validate/attach migration) ✓
- bundle default/cost_hint + tag mutability 결정 정합 ✓

### 작업 완료 항목

- Cargo.toml version 0.5.4 → 0.6.0 bump
- CHANGELOG.md [0.6.0] 항목 — break 항목 모두 정리 (messages[] / warning 제거 / errors→error_count / bundle depth default / cost_hint / tag CLI/MCP / init context)
- cargo test 전체: 111+ tests, 0 fail
- vault cleanup (본 revision 포함): N0090/N0089/N0067/N0086 archived

### v0.6.1 후속 PR 후보 (별 PLAN entry 박지 않고 본 r0005에 메모)

1. **alias 경계 정리** (M-2) — vault_meta init_fallback inject 조건을 launch path → launch resolution 비교로 좁히기. explicit alias 응답엔 부착 X 명시
2. **MCP initialize 응답 검증** (M-3) — process regression test 보강. stdin으로 JSON-RPC initialize 보내고 응답 받기
3. **N0073 비-string 파라미터 역직렬화** — 별 PR (앞서 v0.6.1 후보로 결정)
4. **info messages[] 통합** (Cons-3 묵힘) — hint/next_action/context_hints 등 info성 필드 migrate. 사용자 결정 (i)/(iv)/(v) 받은 후 진행

### git workflow (사용자 명시)

- workbench(origin) push까지 자율
- upstream push는 vault 제외하고 — 메커니즘 사용자 raise 예정 (별 branch 또는 publish 워크플로)
- PR 생략

### 머지 후 사용자 액션

- workbench push 결과 확인
- upstream publish 워크플로 수행 (vault 제외)

[Change] codex 사후 review 결과 + 작업 완료 marker. v0.6.1 후속 PR 후보 4건 메모. git workflow 사용자 결정 인용
[Impact] v0.6.0 PR 머지 직전 상태. 코드 자체 blocker 0, git stage 필요 항목 2건 본 cycle에서 처리. 후속 PR 후보는 vault 메모로만 보존 (별 PLAN entry는 필요 시 splitoff)

→ see N0090 r0005 (흡수 marker — 본 PR 머지 시 archived)
→ see N0086 r0002 (cost cap 결정 marker)
→ see N0080 r0005 (tag mutability 결정 marker)
→ see N0067 r0006 (잔여 close marker)
→ see N0089 (process suicide 회귀 fix — 본 PR로 close)