/target
/.claude
/.idea
/.elendirna
elf_graph.html
elf_ui.py
graph_data.json

# npm distribution — CI-assembled prebuilt binaries (bundled into the single package) + node deps
/npm/elendirna/binaries/
node_modules/
